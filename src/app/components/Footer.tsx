export function Footer() {
  return (
    <footer className="py-9 text-center font-mono text-[12.5px] text-muted-foreground">
      © {new Date().getFullYear()} Ayo Akindele — built from schema up.
    </footer>
  );
}
